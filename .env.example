HOST=0.0.0.0
PORT=8000
WATCH_DIR=./watch_folder          # 要监控的目录
EMBEDDER_BACKEND=local            # local 或 deepseek (或其他)
DEEPSEEK_API_KEY=sk-xxxx          # 仅当 backend 为 deepseek 时需要
VECTOR_DB_PATH=./data/chroma_db
MAX_HEX_BYTES=8192
CHUNK_SIZE=500                    # 最大 token 数（近似字符数）
CHUNK_OVERLAP=50
