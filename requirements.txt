# ============================================================
# FolderRAG — Dependencies
# ============================================================
# Document parsers
pdfminer.six>=20221105
python-docx>=0.8.11
python-pptx>=0.6.21
openpyxl>=3.1.2

# Windows only: python-magic for MIME type detection
python-magic-bin>=0.4.14; platform_system == "Windows"

# GUI dependencies
tkinterdnd2>=0.3.0

# Development
pytest>=7.0
ruff>=0.1.0
