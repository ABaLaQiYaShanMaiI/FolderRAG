# Runtime dependencies (synced with pyproject.toml)
# For `pip install -r requirements.txt`
python-magic>=0.4.27
pdfminer.six>=20221105
python-docx>=0.8.11
python-pptx>=1.0.2
openpyxl>=3.1.5
chardet>=5.0

# Optional GUI dependency (install separately if using GUI)
# tkinterdnd2>=0.3.0

# Dev dependencies
# pytest>=7.0.0